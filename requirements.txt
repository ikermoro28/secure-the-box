# Secure The Box - Defensive CTF Trainer
# ======================================

# GUI Framework (requiere tkinter instalado en el sistema)
customtkinter>=5.2.0

# ⚠️ IMPORTANTE: tkinter debe instalarse a nivel de sistema operativo
# No se puede instalar vía pip en la mayoría de los casos

# Instalación de tkinter según distribución Linux:
# Ubuntu/Debian: sudo apt install python3-tk
# Fedora/RHEL:   sudo dnf install python3-tkinter
# Arch Linux:    sudo pacman -S tk
# openSUSE:      sudo zypper install python3-tk

# Para macOS:
# brew install python-tk

# Para Windows:
# Viene incluido en el instalador oficial de Python

# Módulos estándar de Python (no requieren instalación)
# os, subprocess, threading, sqlite3, json, time
